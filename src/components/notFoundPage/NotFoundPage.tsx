import React from 'react';
import Typography from "@mui/material/Typography";

const NotFoundPage = () => {
    return (
        <Typography>
            Page is doesnt exist
        </Typography>
    );
};

export default NotFoundPage;